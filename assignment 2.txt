#include <stdio.h>
int input();
void output(char name);
int main()
{
	char result;
	int choice;
	printf("Press 1 to Add the candidate\n");
	printf("Press 2 to View the candidate\n");
	printf("Press 3 to Exit\n");
	printf("Enter your choice:\n");
	choice = input();
	
	switch (choice) 
	{
	case 1: 
	{
	    char name[50],candidateID[50],party[50];
	    int division,count,i,n;
	    
	    printf("Enter number of candidates: ");
	    scanf("%d",&n);
	    FILE *fp;
	    fp=fopen("addcandidate.txt","w");
	    for (i=0;i<=n;++i)
	    {
		printf("For candidate%d\nEnter name: ",i+1);
		scanf("%s",name);
		
		printf("Enter candidateID: ");
		scanf("%s",&candidateID);
		
		printf("Enter party: ");
		scanf("%s",&party);
		
		printf("Enter division : ");
		scanf("%d",&division);
		
		printf("Enter count : ");
		scanf("%d",&count);
		
		fprintf(fp,"\nName : %s \ncandidateID : %s \nparty : %s \ndivision : %d \ncount : %d",name,candidateID,party,division,count);
	    }
	fclose(fp);
	    
	    
	    
	    
		break;
	}
	case 2: 
	{
		FILE *fp;
		char ch;
		int i;
		fp = fopen("addcandidate.txt","r");
		ch = fgetc(fp);
		while(ch!=EOF)
		{
			printf("%c",ch);
			ch = fgetc(fp);
		}
		fclose(fp);
		break;
	}
	case 3: 
	{
		printf("EXIT");
		break;
	}
	default:
		printf("wrong Input\n");
	}
	return 0;
}
int input()
{
	int number;
	scanf("%d", &number);
	return (number);
}

void output(char name)
{
	printf("%c", name);
}
